interface LogoProps {
    className?: string;
    onClick?: () => void;
    textClassName?: string;
    imageClassName?: string;
    src?: string;
}
declare const Logo: ({ className, onClick, textClassName, imageClassName, src }: LogoProps) => import("react/jsx-runtime").JSX.Element;
export default Logo;
